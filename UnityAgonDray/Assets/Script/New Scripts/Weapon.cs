using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Weapon : MonoBehaviour
{/*
    public bool noWeapon = true;
    public bool hasStick = false;
    public bool hasSword = false;

    public int noWeaponDamage = 10;
    public int stickDamage = 35;
    public int swordDamage = 50;
    private void OnTriggerEnter(Collider other)
    {
        if(other.tag == "Enemy")
        {
            if (noWeapon)
            {
                other.GetComponent<Enemy>().TakeDamage(noWeaponDamage);
            }
            else if (hasStick)
            {
                other.GetComponent<Enemy>().TakeDamage(stickDamage);
            }
            else if (hasSword)
            {
                other.GetComponent<Enemy>().TakeDamage(swordDamage);
            }
        }
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

    }*/
}
